# Lab 5 — Desafio (em casa)

Você concluiu os 4 labs obrigatórios. Este é opcional — sem entrega separada.

Aqui estão os conteúdos que ficaram de fora dos labs em sala para caber no tempo:

| Arquivo | O que implementar |
|---------|------------------|
| `item.py` | `__radd__`: `heroi + item` retorna novo Personagem com bônus |
| `racas.py` | `Arqueiro` com `atacar()` e acerto crítico (d20 ≥ 18) |
| `inventario.py` | `Inventario(list)` com método `buscar(nome)` |
| `boss_editavel.py` | `atacar()` do Boss — regras livres! |

## Como rodar

```bash
pip install rich
python main.py
```
